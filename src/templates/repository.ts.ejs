<%
/**
 * Extension repository template — written once on
 * `lb-contracts override repository <name>`.
 *
 * View-model fields:
 *   className       string  e.g. "Customer"
 *   baseImportPath  string  relative path to the regenerated base file
 *                            (no `.ts` suffix)
 */
-%>
import {<%- className %>BaseRepository} from '<%- baseImportPath %>';

/**
 * Hand-editable extension of {@link <%- className %>BaseRepository}.
 *
 * The base class is regenerated on every `lb-contracts gen` run. Add custom finders,
 * transaction helpers, or override CRUD methods here; this file is never
 * overwritten by codegen.
 *
 * @public
 */
export class <%- className %>Repository extends <%- className %>BaseRepository {}
